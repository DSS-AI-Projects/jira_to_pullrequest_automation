#server {
#    listen 443 ssl;
#    server_name _;

#    # These paths are correct! They map to the internal container directory.
#    ssl_certificate /etc/nginx/certs/STAR_direction_biz.crt;
#    ssl_certificate_key /etc/nginx/certs/STAR_direction_biz.key;

#    location / {
#        # Route traffic internally to the 'frontend' Docker service
#        proxy_pass http://frontend:3000;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto $scheme;
#    }
#}

server {
    listen 443 ssl;
    server_name _;

    ssl_certificate /etc/nginx/certs/STAR_direction_biz.crt;
    ssl_certificate_key /etc/nginx/certs/STAR_direction_biz.key;

    # Backend API — must come before the catch-all location below.
    location /api/ {
        proxy_pass http://backend:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        proxy_pass http://frontend:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
